// Uses the Vite proxy in development to bypass CORS, and falls back to relative path in production
export const API_BASE_URL = '/api'; 
export const IMAGE_BASE_URL = 'https://backend.techniks.co.nz';
